<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Results</title>
    <link rel="stylesheet" href="/gameResult.css">
</head>
<body>
    <h1>Quiz Results</h1>
    
    <div class="result-container">
        <h2>Your Score: <%= score %> out of <%= totalQuestions %></h2>

        <!-- Progress Bar to visualize the score -->
        <div class="progress-bar">
            <div class="progress" style="width: <%= (score / totalQuestions * 100).toFixed(2) %>%"></div>
        </div>
        

        <!-- Display encouraging message based on score -->
        <p class="result-message">
            <% if (score / totalQuestions > 0.8) { %>
                Great job! You really know the Constitution!
            <% } else if (score / totalQuestions > 0.5) { %>
                Good effort! Keep learning!
            <% } else { %>
                Don't worry! Try again and improve your knowledge!
            <% } %>
        </p>

        <a href="/quiz" class="btn">Try Again</a>
        <a href="/" class="btn">Back to Home</a>
    </div>
</body>
</html>
